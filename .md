CREATE TABLE departments(
id SERIAL PRIMARY KEY,
name VARCHAR(100) NOT NULL
);
CREATE TABLE users(
id SERIAL PRIMARY KEY,
name VARCHAR(100) NOT NULL,
email VARCHAR(150),
role VARCHAR(50) NOT NULL CHECK(role In('student','professor','admin')),
department_id INT REFERENCES departments(id) ON DELETE SET NULL,
metadata JSONB,
is_active BOOLEAN,
create_at TIMESTAMP DEFAULT NOW()
);
SELECT *
FROM pg_tables
WHERE schemaname = 'public';

DROP TABLE users;

CREATE TABLE courses(
id SERIAL PRIMARY KEY,
title VARCHAR(100),
fee NUMERIC(10,2)
);
--single record---
insert into departments (name) values('Computer science');
select * from departments;
select * from users;
--mulltiple record---
insert into users(name,email,role) values ('namdev','namdev@6266','student');
insert  into departments(id,name) values(2,'mechnical Engineering'),(3,'Electronice &Communication'),(4,'Business Administration')
RETURNING id;
--return users table---
select * from users;
select name ,email,role from users;
select id,name from departments;
select name AS fullName , email AS contact from users
select name AS "full Name" ,email AS "contact address" from users;
